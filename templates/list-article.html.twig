{% extends 'base.html.twig' %}

        {% block metatitle %}
        <title>List-article</title>
        {% endblock metatitle %}

{% block main %}

<main>

    <section>
	
		{% for article in articles %}

			<article>
				<p>{{ article.title }}</p>
				<p>{{ article.description }}</p>
				<img src="{{ article.image }}" alt="{{ article.title }}" />
				<p>{{ article.content }}</p>
				<p>Créé le {{ article.createdAt | date()}}</p>
			</article>

		{% endfor %}

	</section>

	<!-- <h1>All articles :</h1> -->
    <!-- {% for article in articles %}
    <h2>{{ article.title }}</h2>
            <div class="article-card"><img src="{{ asset(article.image) }}" alt="Photo de {{ article.title }}" /></div>
            <p>{{ article.description }}</p>
            <p>{{ article.content }}</p>
            <p>{{ article.createdAt | date('d-m-Y')}}</p>
            <p>{{ article.isPublished }}</p>
            
    {% endfor %} -->


</main>
{% endblock main %}